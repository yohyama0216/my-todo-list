---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="お問い合わせ | ポートフォリオ"
  description="デザイン制作、システム開発に関するお問い合わせはこちらから。お気軽にご相談ください。"
>
  <section class="hero">
    <div class="container">
      <h1>お問い合わせ</h1>
      <p class="intro">デザイン制作、システム開発に関するご相談はお気軽にどうぞ。</p>
    </div>
  </section>

  <section class="contact-section">
    <div class="container">
      
      <div class="contact-wrapper">
        
        <!-- Contact Form -->
        <div class="contact-form-container">
          <form class="contact-form" action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
            
            <!-- Service Type Selection -->
            <div class="form-group">
              <label for="service-type">ご希望のサービス <span class="required">*</span></label>
              <select id="service-type" name="service-type" required>
                <option value="">選択してください</option>
                <option value="design">デザイン制作（チラシ・ポスター）</option>
                <option value="development">システム開発（PHP）</option>
                <option value="both">両方について相談</option>
                <option value="other">その他</option>
              </select>
            </div>

            <!-- Inquiry Type -->
            <div class="form-group">
              <label for="inquiry-type">お問い合わせ内容 <span class="required">*</span></label>
              <select id="inquiry-type" name="inquiry-type" required>
                <option value="">選択してください</option>
                <option value="estimate">見積もり依頼</option>
                <option value="consultation">相談・質問</option>
                <option value="request">制作・開発依頼</option>
                <option value="other">その他</option>
              </select>
            </div>

            <!-- Name -->
            <div class="form-group">
              <label for="name">お名前 <span class="required">*</span></label>
              <input type="text" id="name" name="name" required placeholder="山田 太郎">
            </div>

            <!-- Email -->
            <div class="form-group">
              <label for="email">メールアドレス <span class="required">*</span></label>
              <input type="email" id="email" name="email" required placeholder="example@email.com">
            </div>

            <!-- Company (Optional) -->
            <div class="form-group">
              <label for="company">会社名・団体名（任意）</label>
              <input type="text" id="company" name="company" placeholder="株式会社サンプル">
            </div>

            <!-- Phone (Optional) -->
            <div class="form-group">
              <label for="phone">電話番号（任意）</label>
              <input type="tel" id="phone" name="phone" placeholder="090-1234-5678">
            </div>

            <!-- Budget (Optional) -->
            <div class="form-group">
              <label for="budget">ご予算（任意）</label>
              <select id="budget" name="budget">
                <option value="">選択してください</option>
                <option value="under-50k">5万円以下</option>
                <option value="50k-100k">5万円〜10万円</option>
                <option value="100k-300k">10万円〜30万円</option>
                <option value="300k-500k">30万円〜50万円</option>
                <option value="500k-1m">50万円〜100万円</option>
                <option value="over-1m">100万円以上</option>
                <option value="not-decided">未定</option>
              </select>
            </div>

            <!-- Deadline (Optional) -->
            <div class="form-group">
              <label for="deadline">希望納期（任意）</label>
              <input type="text" id="deadline" name="deadline" placeholder="例: 1ヶ月以内、2024年12月末まで">
            </div>

            <!-- Message -->
            <div class="form-group">
              <label for="message">お問い合わせ内容の詳細 <span class="required">*</span></label>
              <textarea id="message" name="message" rows="8" required placeholder="プロジェクトの詳細、ご要望、ご質問などをお聞かせください。

例：
- どのようなチラシを作りたいか
- システムの現状と改善したい点
- 参考となるデザインやサイト
- その他ご要望"></textarea>
            </div>

            <!-- Privacy Policy Agreement -->
            <div class="form-group checkbox-group">
              <label class="checkbox-label">
                <input type="checkbox" name="privacy-agreed" required>
                <span>個人情報の取り扱いに同意する <span class="required">*</span></span>
              </label>
              <p class="privacy-note">
                お預かりした個人情報は、お問い合わせへの回答および関連するご連絡のみに使用し、第三者に提供することはございません。
              </p>
            </div>

            <!-- Submit Button -->
            <div class="form-group">
              <button type="submit" class="btn btn-primary btn-submit">送信する</button>
            </div>

          </form>
        </div>

        <!-- Contact Info Sidebar -->
        <div class="contact-info">
          <div class="info-card">
            <h3>📧 メールでのお問い合わせ</h3>
            <p>フォーム送信後、通常1〜2営業日以内にご返信いたします。</p>
          </div>

          <div class="info-card">
            <h3>⏰ 対応時間</h3>
            <p>平日 10:00 - 18:00<br>
            （土日祝日は休業）</p>
          </div>

          <div class="info-card">
            <h3>💡 お見積もりについて</h3>
            <p>概算見積もりは無料です。プロジェクトの詳細をお聞かせいただければ、できるだけ早くお見積もりをご提示いたします。</p>
          </div>

          <div class="info-card">
            <h3>📝 事前にご用意いただけると助かる情報</h3>
            <ul>
              <li>プロジェクトの目的</li>
              <li>ターゲット層</li>
              <li>希望納期</li>
              <li>ご予算</li>
              <li>参考資料（あれば）</li>
            </ul>
          </div>
        </div>

      </div>

    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 4rem 0;
    text-align: center;
  }

  .hero h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: white;
  }

  .intro {
    font-size: 1.125rem;
    opacity: 0.95;
    margin-bottom: 0;
  }

  .contact-section {
    padding: 4rem 0;
  }

  .contact-wrapper {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 3rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .contact-form-container {
    background-color: white;
    padding: 2rem;
    border-radius: 0.5rem;
    box-shadow: var(--shadow);
  }

  .contact-form {
    max-width: 100%;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  label {
    display: block;
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--text-dark);
  }

  .required {
    color: #ef4444;
  }

  input[type="text"],
  input[type="email"],
  input[type="tel"],
  select,
  textarea {
    width: 100%;
    padding: 0.75rem;
    border: 2px solid var(--border-color);
    border-radius: 0.375rem;
    font-size: 1rem;
    transition: border-color 0.2s;
    font-family: inherit;
  }

  input[type="text"]:focus,
  input[type="email"]:focus,
  input[type="tel"]:focus,
  select:focus,
  textarea:focus {
    outline: none;
    border-color: var(--primary-color);
  }

  textarea {
    resize: vertical;
    min-height: 150px;
  }

  .checkbox-group {
    padding: 1rem;
    background-color: var(--bg-light);
    border-radius: 0.375rem;
  }

  .checkbox-label {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    cursor: pointer;
    font-weight: normal;
    margin-bottom: 0.5rem;
  }

  .checkbox-label input[type="checkbox"] {
    margin-top: 0.25rem;
    width: auto;
    cursor: pointer;
  }

  .privacy-note {
    font-size: 0.875rem;
    color: var(--text-gray);
    margin-top: 0.5rem;
    margin-bottom: 0;
    line-height: 1.5;
  }

  .btn-submit {
    width: 100%;
    font-size: 1.125rem;
    padding: 1rem;
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .info-card {
    background-color: white;
    padding: 1.5rem;
    border-radius: 0.5rem;
    box-shadow: var(--shadow);
  }

  .info-card h3 {
    font-size: 1.125rem;
    margin-bottom: 1rem;
    color: var(--text-dark);
  }

  .info-card p {
    color: var(--text-gray);
    line-height: 1.6;
    margin-bottom: 0;
  }

  .info-card ul {
    list-style: none;
    padding-left: 0;
  }

  .info-card li {
    padding: 0.25rem 0;
    color: var(--text-gray);
    padding-left: 1.25rem;
    position: relative;
  }

  .info-card li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: var(--primary-color);
    font-weight: bold;
  }

  @media (max-width: 992px) {
    .contact-wrapper {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .contact-info {
      order: -1;
    }
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 1.75rem;
    }

    .intro {
      font-size: 1rem;
    }

    .contact-section {
      padding: 2rem 0;
    }

    .contact-form-container {
      padding: 1.5rem;
    }

    .contact-wrapper {
      gap: 1.5rem;
    }
  }
</style>
