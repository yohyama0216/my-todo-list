---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="お問い合わせ - デザイン＆開発サービス"
  description="チラシ・ポスター制作、PHPシステム開発のご相談、お見積もり依頼はこちらから。お気軽にお問い合わせください。"
>
  <section class="page-header">
    <div class="container">
      <h1>お問い合わせ</h1>
      <p class="lead">まずはお気軽にご相談ください</p>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="contact-content">
        <div class="contact-info">
          <h2>ご相談の前に</h2>
          <div class="info-box">
            <h3>こんな方はお気軽にどうぞ</h3>
            <ul>
              <li>要件がまだ固まっていない</li>
              <li>予算感を知りたい</li>
              <li>技術選定で悩んでいる</li>
              <li>まずは相談だけしたい</li>
            </ul>
          </div>

          <div class="info-box">
            <h3>返信について</h3>
            <p>
              通常、24時間以内にご返信いたします。<br />
              土日祝日の場合は翌営業日の返信となる場合があります。
            </p>
          </div>

          <div class="info-box">
            <h3>対応可能な範囲</h3>
            <p><strong>デザイン:</strong></p>
            <ul>
              <li>チラシ・ポスター制作</li>
              <li>バナー・SNS画像</li>
              <li>イベント告知デザイン</li>
            </ul>
            <p><strong>開発:</strong></p>
            <ul>
              <li>PHPシステム開発・改修</li>
              <li>WordPress カスタマイズ</li>
              <li>管理画面・業務システム</li>
              <li>API開発・連携</li>
            </ul>
          </div>
        </div>

        <div class="contact-form-wrapper">
          <!-- 
            重要: Formspreeの設定
            1. https://formspree.io/ でアカウントを作成
            2. 新しいフォームを作成してフォームIDを取得
            3. 下記の YOUR_FORM_ID を実際のIDに置き換えてください
            例: action="https://formspree.io/f/xwkgpzyz"
          -->
          <form 
            id="contact-form" 
            action="https://formspree.io/f/YOUR_FORM_ID" 
            method="POST"
            class="contact-form"
          >
            <div class="form-group">
              <label for="service-type">ご相談サービス <span class="required">*</span></label>
              <select id="service-type" name="service-type" required>
                <option value="">選択してください</option>
                <option value="design">デザイン相談（チラシ・ポスター制作）</option>
                <option value="dev">開発相談（PHPシステム開発）</option>
                <option value="both">両方について相談したい</option>
              </select>
            </div>

            <div class="form-group">
              <label for="name">お名前 <span class="required">*</span></label>
              <input 
                type="text" 
                id="name" 
                name="name" 
                required 
                placeholder="山田太郎"
              />
            </div>

            <div class="form-group">
              <label for="email">メールアドレス <span class="required">*</span></label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                required 
                placeholder="example@example.com"
              />
            </div>

            <div class="form-group">
              <label for="company">会社名・団体名</label>
              <input 
                type="text" 
                id="company" 
                name="company" 
                placeholder="株式会社サンプル"
              />
            </div>

            <div class="form-group">
              <label for="project-type">具体的なご相談内容 <span class="required">*</span></label>
              <select id="project-type" name="project-type" required>
                <option value="">選択してください</option>
                <optgroup label="デザイン関連">
                  <option value="チラシ制作">チラシ制作</option>
                  <option value="ポスター制作">ポスター制作</option>
                  <option value="バナー・SNS画像">バナー・SNS画像</option>
                  <option value="その他デザイン">その他デザイン</option>
                </optgroup>
                <optgroup label="開発関連">
                  <option value="既存システム改修">既存システム改修</option>
                  <option value="新規システム開発">新規システム開発</option>
                  <option value="WordPress関連">WordPress関連</option>
                  <option value="API開発・連携">API開発・連携</option>
                  <option value="保守・サポート">保守・サポート</option>
                  <option value="その他開発">その他開発</option>
                </optgroup>
                <option value="その他">その他</option>
              </select>
            </div>

            <div class="form-group">
              <label for="budget">ご予算</label>
              <select id="budget" name="budget">
                <option value="">選択してください</option>
                <option value="10万円未満">10万円未満</option>
                <option value="10万円〜30万円">10万円〜30万円</option>
                <option value="30万円〜50万円">30万円〜50万円</option>
                <option value="50万円〜100万円">50万円〜100万円</option>
                <option value="100万円以上">100万円以上</option>
                <option value="未定">未定</option>
              </select>
            </div>

            <div class="form-group">
              <label for="timeline">希望納期</label>
              <input 
                type="text" 
                id="timeline" 
                name="timeline" 
                placeholder="例：1ヶ月以内、3月末まで、未定"
              />
            </div>

            <div class="form-group">
              <label for="message">お問い合わせ内容 <span class="required">*</span></label>
              <textarea 
                id="message" 
                name="message" 
                rows="8" 
                required
                placeholder="プロジェクトの概要、課題、ご要望などをお聞かせください。&#10;&#10;【デザインの場合】&#10;・目的（例：新規オープン告知、イベント集客など）&#10;・ターゲット層&#10;・希望する雰囲気・イメージ&#10;・使用場所（印刷、Web、SNSなど）&#10;&#10;【開発の場合】&#10;・解決したい課題&#10;・現在のシステム環境（わかる範囲で）&#10;・実現したい機能"
              ></textarea>
            </div>

            <div class="form-actions">
              <button type="submit" class="btn btn-primary btn-large">
                送信する
              </button>
            </div>

            <p class="form-note">
              <small>
                送信いただいた情報は、お問い合わせ対応のみに使用し、第三者に提供することはありません。
              </small>
            </p>
          </form>

          <div id="form-success" class="form-message success" style="display: none;">
            <h3>送信完了</h3>
            <p>お問い合わせありがとうございます。<br />24時間以内にご返信いたします。</p>
          </div>

          <div id="form-error" class="form-message error" style="display: none;">
            <h3>送信エラー</h3>
            <p>申し訳ございません。送信中にエラーが発生しました。<br />お手数ですが、時間をおいて再度お試しください。</p>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header {
    background-color: var(--bg-light);
    padding: var(--spacing-lg) 0;
    text-align: center;
  }

  .page-header h1 {
    margin-bottom: var(--spacing-sm);
  }

  .lead {
    font-size: 1.25rem;
    color: var(--text-gray);
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: var(--spacing-lg);
    max-width: 1200px;
    margin: 0 auto;
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .info-box {
    background-color: var(--bg-light);
    padding: var(--spacing-md);
    border-radius: 0.5rem;
  }

  .info-box h3 {
    color: var(--primary-color);
    margin-bottom: var(--spacing-sm);
    font-size: 1.125rem;
  }

  .info-box ul {
    list-style: none;
    padding-left: 0;
  }

  .info-box li {
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
  }

  .info-box li:before {
    content: "✓";
    color: var(--primary-color);
    font-weight: bold;
    position: absolute;
    left: 0;
  }

  .info-box p {
    margin-bottom: 0;
    line-height: 1.8;
  }

  .contact-form-wrapper {
    background-color: var(--bg-white);
    padding: var(--spacing-lg);
    border-radius: 0.5rem;
    box-shadow: var(--shadow);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-group label {
    font-weight: 600;
    margin-bottom: 0.5rem;
    color: var(--text-dark);
  }

  .required {
    color: #dc2626;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 0.75rem;
    border: 1px solid var(--border-color);
    border-radius: 0.375rem;
    font-size: 1rem;
    font-family: inherit;
    transition: border-color 0.2s;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 150px;
  }

  .form-actions {
    margin-top: var(--spacing-sm);
  }

  .btn-large {
    width: 100%;
    padding: 1rem;
    font-size: 1.125rem;
  }

  .form-note {
    text-align: center;
    color: var(--text-gray);
    margin-top: var(--spacing-sm);
    margin-bottom: 0;
  }

  .form-message {
    padding: var(--spacing-lg);
    border-radius: 0.5rem;
    text-align: center;
  }

  .form-message.success {
    background-color: #d1fae5;
    border: 2px solid #10b981;
    color: #065f46;
  }

  .form-message.error {
    background-color: #fee2e2;
    border: 2px solid #ef4444;
    color: #991b1b;
  }

  .form-message h3 {
    margin-bottom: var(--spacing-sm);
  }

  .form-message p {
    margin-bottom: 0;
  }

  @media (max-width: 768px) {
    .contact-content {
      grid-template-columns: 1fr;
    }

    .contact-form-wrapper {
      padding: var(--spacing-md);
    }
  }
</style>

<script>
  // Pre-select service type from URL parameter
  const urlParams = new URLSearchParams(window.location.search);
  const serviceParam = urlParams.get('service');
  
  if (serviceParam) {
    const serviceSelect = document.getElementById('service-type') as HTMLSelectElement;
    if (serviceSelect) {
      serviceSelect.value = serviceParam;
    }
  }

  // Simple form handling (replace with actual Formspree integration)
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const successMessage = document.getElementById('form-success');
  const errorMessage = document.getElementById('form-error');

  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      try {
        const formData = new FormData(form);
        
        // Replace YOUR_FORM_ID with actual Formspree form ID
        // For now, just show success message
        form.style.display = 'none';
        if (successMessage) {
          successMessage.style.display = 'block';
        }
        
        // Uncomment when using actual Formspree:
        /*
        const response = await fetch(form.action, {
          method: 'POST',
          body: formData,
          headers: {
            'Accept': 'application/json'
          }
        });
        
        if (response.ok) {
          form.style.display = 'none';
          if (successMessage) {
            successMessage.style.display = 'block';
          }
        } else {
          throw new Error('Form submission failed');
        }
        */
      } catch (error) {
        form.style.display = 'none';
        if (errorMessage) {
          errorMessage.style.display = 'block';
        }
      }
    });
  }
</script>
