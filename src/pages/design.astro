---
import BaseLayout from '../layouts/BaseLayout.astro';
import CTAButton from '../components/CTAButton.astro';
---

<BaseLayout 
  title="チラシ・ポスター制作 | デザインサービス"
  description="店舗、イベント、広告向けのチラシ・ポスター制作。ターゲットに響くデザインで集客・認知度向上をサポート。納品形式・料金目安も明確。"
>
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero-tag">Design</div>
      <h1 class="hero-title">チラシ・ポスター制作</h1>
      <p class="hero-subtitle">
        ターゲットに響くデザインで、集客・認知度向上をサポート
      </p>
    </div>
  </section>

  <!-- Service Overview -->
  <section class="section service-overview">
    <div class="container">
      <h2 class="section-title">こんな方におすすめ</h2>
      <div class="target-grid">
        <div class="target-item">
          <div class="target-icon">🏪</div>
          <h3>店舗経営者</h3>
          <p>新規オープン、セール告知、メニュー表など</p>
        </div>
        <div class="target-item">
          <div class="target-icon">🎉</div>
          <h3>イベント主催者</h3>
          <p>音楽イベント、展示会、地域イベントの告知</p>
        </div>
        <div class="target-item">
          <div class="target-icon">💼</div>
          <h3>中小企業・個人事業主</h3>
          <p>サービス紹介、採用案内、キャンペーン告知</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Works Gallery -->
  <section class="section works-gallery">
    <div class="container">
      <h2 class="section-title">制作実績</h2>
      <div class="gallery-grid">
        
        <!-- Work 1 -->
        <div class="work-item" id="work-cafe-flyer">
          <div class="work-image">
            <div class="placeholder-image">🎨</div>
          </div>
          <div class="work-content">
            <h3>カフェオープンチラシ</h3>
            <div class="work-meta">
              <p><strong>目的:</strong> 新規カフェの認知度向上とオープン告知</p>
              <p><strong>ターゲット:</strong> 20-40代女性、カフェ好き、地域住民</p>
              <p><strong>工夫:</strong> 温かみのある配色とイラストで親しみやすさを演出。メニューの写真を大きく配置し、視覚的な訴求力を重視。</p>
              <p><strong>成果:</strong> 配布後の来店率120%向上、SNS投稿50件以上</p>
              <p><strong>納品形式:</strong> PDF（印刷用）、PNG（Web用）</p>
            </div>
            <div class="work-tags">
              <span class="tag">チラシデザイン</span>
              <span class="tag">Illustrator</span>
              <span class="tag">カフェ</span>
            </div>
          </div>
        </div>

        <!-- Work 2 -->
        <div class="work-item" id="work-music-poster">
          <div class="work-image">
            <div class="placeholder-image">🎸</div>
          </div>
          <div class="work-content">
            <h3>音楽イベントポスター</h3>
            <div class="work-meta">
              <p><strong>目的:</strong> 地域音楽フェスティバルの集客</p>
              <p><strong>ターゲット:</strong> 10-30代音楽ファン、地域住民</p>
              <p><strong>工夫:</strong> 力強いタイポグラフィとダイナミックな構図で、エネルギッシュな雰囲気を表現。遠くからでも目を引くデザイン。</p>
              <p><strong>成果:</strong> 前年比チケット売上150%、SNSシェア200件以上</p>
              <p><strong>納品形式:</strong> AI、PDF（印刷用）、JPEG（Web/SNS用）</p>
            </div>
            <div class="work-tags">
              <span class="tag">ポスターデザイン</span>
              <span class="tag">Photoshop</span>
              <span class="tag">イベント</span>
            </div>
          </div>
        </div>

        <!-- Work 3 -->
        <div class="work-item" id="work-real-estate-banner">
          <div class="work-image">
            <div class="placeholder-image">🏡</div>
          </div>
          <div class="work-content">
            <h3>不動産会社の広告バナー</h3>
            <div class="work-meta">
              <p><strong>目的:</strong> Web広告のクリック率向上</p>
              <p><strong>ターゲット:</strong> 30-50代、住宅購入検討者</p>
              <p><strong>工夫:</strong> 信頼感のある配色と明瞭なキャッチコピー。複数サイズ（PC/SP）で最適化。A/Bテストで最良デザインを選定。</p>
              <p><strong>成果:</strong> CTR2.5倍改善、問い合わせ数180%増加</p>
              <p><strong>納品形式:</strong> PNG（各サイズ）、元データ（PSD）</p>
            </div>
            <div class="work-tags">
              <span class="tag">バナーデザイン</span>
              <span class="tag">Web広告</span>
              <span class="tag">不動産</span>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- Pricing & Details -->
  <section class="section pricing-details">
    <div class="container">
      <h2 class="section-title">料金・納期の目安</h2>
      
      <div class="pricing-grid">
        <div class="pricing-card">
          <h3>チラシデザイン</h3>
          <div class="price">¥15,000〜</div>
          <ul class="pricing-features">
            <li>A4サイズ片面・両面対応</li>
            <li>修正2回まで無料</li>
            <li>印刷用PDF + Web用PNG納品</li>
          </ul>
          <p class="pricing-time">納期: 1〜2週間</p>
        </div>

        <div class="pricing-card">
          <h3>ポスターデザイン</h3>
          <div class="price">¥20,000〜</div>
          <ul class="pricing-features">
            <li>A3/B2サイズ対応</li>
            <li>修正2回まで無料</li>
            <li>印刷用AI/PDF + Web用JPEG納品</li>
          </ul>
          <p class="pricing-time">納期: 2〜3週間</p>
        </div>

        <div class="pricing-card">
          <h3>バナー・SNS画像</h3>
          <div class="price">¥8,000〜</div>
          <ul class="pricing-features">
            <li>各種サイズ対応</li>
            <li>修正1回まで無料</li>
            <li>Web用PNG納品</li>
          </ul>
          <p class="pricing-time">納期: 3〜7日</p>
        </div>
      </div>

      <div class="additional-info">
        <h3>制作の流れ</h3>
        <div class="flow-steps">
          <div class="flow-step">
            <div class="step-number">1</div>
            <h4>ヒアリング</h4>
            <p>目的、ターゲット、希望イメージ、納期などをお伺いします</p>
          </div>
          <div class="flow-step">
            <div class="step-number">2</div>
            <h4>デザイン案提示</h4>
            <p>ラフ案を2-3パターン提示し、方向性を決定します</p>
          </div>
          <div class="flow-step">
            <div class="step-number">3</div>
            <h4>修正・調整</h4>
            <p>フィードバックをもとに修正（2回まで無料）</p>
          </div>
          <div class="flow-step">
            <div class="step-number">4</div>
            <h4>納品</h4>
            <p>印刷用・Web用データを納品します</p>
          </div>
        </div>

        <div class="notes">
          <h4>注意事項</h4>
          <ul>
            <li>修正回数は2回まで無料。3回目以降は追加料金が発生します</li>
            <li>印刷手配は別途ご相談ください（提携印刷会社のご紹介可能）</li>
            <li>写真素材の手配が必要な場合、別途ストックフォト費用が発生する場合があります</li>
            <li>納期は内容により変動します。お急ぎの場合はご相談ください</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <h2>デザイン制作サービス</h2>
      <p>チラシ・ポスター制作のサービスを提供しています。</p>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero Section - Flyer Style */
  .hero {
    background: linear-gradient(135deg, #ff6b9d 0%, #c94b4b 50%, #ff8e53 100%);
    color: white;
    padding: 5rem 0 4rem;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '🎨';
    position: absolute;
    font-size: 20rem;
    opacity: 0.1;
    top: -5rem;
    right: -5rem;
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(5deg); }
  }

  .hero-tag {
    display: inline-block;
    background-color: rgba(255, 255, 255, 0.3);
    padding: 0.5rem 1.5rem;
    border-radius: 2rem;
    font-size: 1rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--spacing-sm);
    border: 2px solid rgba(255, 255, 255, 0.5);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  }

  .hero-title {
    font-size: 3.5rem;
    margin-bottom: var(--spacing-sm);
    text-shadow: 2px 4px 8px rgba(0, 0, 0, 0.3);
    font-weight: 900;
    position: relative;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    opacity: 0.95;
    font-weight: 500;
    max-width: 800px;
    margin: 0 auto;
    text-shadow: 1px 2px 4px rgba(0, 0, 0, 0.2);
  }

  /* Target Audience - Flyer Card Style */
  .service-overview {
    background: linear-gradient(to bottom, #fff5f7 0%, #ffe4e6 100%);
    position: relative;
  }

  .service-overview::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(to right, #ff6b9d, #c94b4b, #ff8e53);
  }

  .target-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-md);
  }

  .target-item {
    background: linear-gradient(135deg, #ffffff 0%, #fff5f7 100%);
    padding: var(--spacing-md);
    border-radius: 1rem;
    box-shadow: 0 8px 25px rgba(255, 107, 157, 0.2);
    text-align: center;
    border: 3px solid #ffccd5;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .target-item::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(255, 107, 157, 0.1) 0%, transparent 70%);
    transform: rotate(45deg);
  }

  .target-item:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 12px 35px rgba(255, 107, 157, 0.3);
    border-color: #ff6b9d;
  }

  .target-icon {
    font-size: 4rem;
    margin-bottom: var(--spacing-sm);
    filter: drop-shadow(2px 2px 4px rgba(0, 0, 0, 0.1));
    position: relative;
    z-index: 1;
  }

  .target-item h3 {
    margin-bottom: var(--spacing-xs);
    color: #c94b4b;
    font-weight: 700;
    position: relative;
    z-index: 1;
  }

  .target-item p {
    color: var(--text-gray);
    margin-bottom: 0;
    position: relative;
    z-index: 1;
  }

  /* Works Gallery - Magazine Layout */
  .works-gallery {
    background: white;
    position: relative;
  }

  .gallery-grid {
    display: grid;
    gap: var(--spacing-xl);
  }

  .work-item {
    display: grid;
    grid-template-columns: 400px 1fr;
    gap: var(--spacing-lg);
    background: linear-gradient(to right, #fff 0%, #fef3f4 100%);
    border-radius: 1.5rem;
    overflow: hidden;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
    border: 3px solid #ffe4e6;
    transition: all 0.3s ease;
  }

  .work-item:hover {
    transform: translateX(10px);
    box-shadow: 0 15px 50px rgba(255, 107, 157, 0.25);
    border-color: #ffccd5;
  }

  .work-image {
    background: linear-gradient(135deg, #ffc2d1 0%, #ffaec5 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 350px;
    position: relative;
    overflow: hidden;
  }

  .work-image::before {
    content: '';
    position: absolute;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.3) 0%, transparent 70%);
    animation: pulse 3s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); opacity: 0.5; }
    50% { transform: scale(1.1); opacity: 0.8; }
  }

  .placeholder-image {
    font-size: 5rem;
    opacity: 0.7;
    filter: drop-shadow(4px 4px 8px rgba(0, 0, 0, 0.2));
    z-index: 1;
    position: relative;
  }

  .work-content {
    padding: var(--spacing-lg);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .work-content h3 {
    margin-bottom: var(--spacing-md);
    color: #c94b4b;
    font-size: 1.75rem;
    font-weight: 800;
    border-bottom: 4px solid #ffe4e6;
    padding-bottom: var(--spacing-xs);
  }

  .work-meta {
    margin-bottom: var(--spacing-md);
    background: rgba(255, 228, 230, 0.3);
    padding: var(--spacing-md);
    border-radius: 0.75rem;
    border-left: 5px solid #ff6b9d;
  }

  .work-meta p {
    margin-bottom: var(--spacing-sm);
    color: var(--text-dark);
    line-height: 1.7;
  }

  .work-meta p:last-child {
    margin-bottom: 0;
  }

  .work-meta strong {
    color: #c94b4b;
    font-weight: 700;
    background: linear-gradient(to right, #ffe4e6, transparent);
    padding: 0.1rem 0.3rem;
    border-radius: 0.25rem;
  }

  .work-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .tag {
    display: inline-block;
    background: linear-gradient(135deg, #ff6b9d 0%, #ff8e53 100%);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    font-size: 0.875rem;
    font-weight: 600;
    box-shadow: 0 4px 10px rgba(255, 107, 157, 0.3);
    transition: all 0.3s ease;
  }

  .tag:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 15px rgba(255, 107, 157, 0.4);
  }

  /* Pricing - Flyer Style Cards */
  .pricing-details {
    background: linear-gradient(to bottom, #fff5f7 0%, white 100%);
    position: relative;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-xl);
  }

  .pricing-card {
    background: white;
    padding: var(--spacing-lg);
    border-radius: 1.5rem;
    box-shadow: 0 10px 30px rgba(255, 107, 157, 0.15);
    text-align: center;
    border: 4px solid #ffe4e6;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .pricing-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: linear-gradient(to right, #ff6b9d, #ff8e53);
  }

  .pricing-card:hover {
    transform: translateY(-10px) scale(1.05);
    box-shadow: 0 20px 50px rgba(255, 107, 157, 0.3);
    border-color: #ff6b9d;
  }

  .pricing-card h3 {
    margin-bottom: var(--spacing-md);
    color: #c94b4b;
    font-weight: 800;
    font-size: 1.5rem;
  }

  .price {
    font-size: 2.5rem;
    font-weight: 900;
    color: #ff6b9d;
    margin-bottom: var(--spacing-md);
    text-shadow: 2px 2px 4px rgba(255, 107, 157, 0.2);
  }

  .pricing-features {
    list-style: none;
    padding: 0;
    margin-bottom: var(--spacing-md);
    text-align: left;
  }

  .pricing-features li {
    padding: var(--spacing-xs) 0;
    color: var(--text-gray);
    position: relative;
    padding-left: 2rem;
    font-weight: 500;
  }

  .pricing-features li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: #ff6b9d;
    font-weight: bold;
    font-size: 1.5rem;
  }

  .pricing-time {
    color: white;
    font-size: 0.875rem;
    margin-bottom: 0;
    background: linear-gradient(135deg, #ff6b9d 0%, #ff8e53 100%);
    padding: var(--spacing-xs) var(--spacing-sm);
    border-radius: 2rem;
    font-weight: 600;
    box-shadow: 0 4px 10px rgba(255, 107, 157, 0.3);
  }

  /* Additional Info - Flow Steps */
  .additional-info {
    margin-top: var(--spacing-xl);
  }

  .additional-info h3 {
    margin-bottom: var(--spacing-md);
    color: #c94b4b;
    font-size: 1.75rem;
    font-weight: 800;
    text-align: center;
  }

  .flow-steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-lg);
  }

  .flow-step {
    text-align: center;
    padding: var(--spacing-lg);
    background: white;
    border-radius: 1rem;
    box-shadow: 0 6px 20px rgba(255, 107, 157, 0.15);
    border: 3px solid #ffe4e6;
    transition: all 0.3s ease;
  }

  .flow-step:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 30px rgba(255, 107, 157, 0.25);
    border-color: #ff6b9d;
  }

  .step-number {
    width: 4rem;
    height: 4rem;
    background: linear-gradient(135deg, #ff6b9d 0%, #ff8e53 100%);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    font-weight: 900;
    margin: 0 auto var(--spacing-sm);
    box-shadow: 0 6px 20px rgba(255, 107, 157, 0.4);
  }

  .flow-step h4 {
    margin-bottom: var(--spacing-xs);
    color: #c94b4b;
    font-weight: 700;
  }

  .flow-step p {
    color: var(--text-gray);
    font-size: 0.875rem;
    margin-bottom: 0;
    line-height: 1.6;
  }

  .notes {
    background: linear-gradient(135deg, #fff5f7 0%, #ffe4e6 100%);
    padding: var(--spacing-lg);
    border-radius: 1rem;
    border-left: 6px solid #ff6b9d;
    box-shadow: 0 6px 20px rgba(255, 107, 157, 0.15);
  }

  .notes h4 {
    margin-bottom: var(--spacing-sm);
    color: #c94b4b;
    font-weight: 800;
  }

  .notes ul {
    margin-bottom: 0;
    padding-left: 1.5rem;
  }

  .notes li {
    color: var(--text-dark);
    margin-bottom: var(--spacing-sm);
    font-weight: 500;
  }

  /* CTA Section - Bold Flyer Style */
  .cta-section {
    background: linear-gradient(135deg, #ff6b9d 0%, #c94b4b 50%, #ff8e53 100%);
    color: white;
    text-align: center;
    padding: 5rem 0;
    position: relative;
    overflow: hidden;
  }

  .cta-section::before {
    content: '✨';
    position: absolute;
    font-size: 15rem;
    opacity: 0.1;
    top: -3rem;
    left: -3rem;
  }

  .cta-section::after {
    content: '🎨';
    position: absolute;
    font-size: 15rem;
    opacity: 0.1;
    bottom: -3rem;
    right: -3rem;
  }

  .cta-section h2 {
    margin-bottom: var(--spacing-sm);
    color: white;
    font-size: 2.5rem;
    font-weight: 900;
    text-shadow: 2px 4px 8px rgba(0, 0, 0, 0.3);
    position: relative;
    z-index: 1;
  }

  .cta-section p {
    font-size: 1.25rem;
    margin-bottom: var(--spacing-md);
    opacity: 0.95;
    position: relative;
    z-index: 1;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.25rem;
    }

    .hero-subtitle {
      font-size: 1.125rem;
    }

    .hero::before {
      font-size: 10rem;
    }

    .work-item {
      grid-template-columns: 1fr;
    }

    .work-image {
      min-height: 250px;
    }

    .target-grid {
      grid-template-columns: 1fr;
    }

    .pricing-grid {
      grid-template-columns: 1fr;
    }

    .flow-steps {
      grid-template-columns: 1fr;
    }

    .cta-section::before,
    .cta-section::after {
      font-size: 8rem;
    }
  }
</style>
