---
import BaseLayout from '../layouts/BaseLayout.astro';
import CTAButton from '../components/CTAButton.astro';
import ServiceCard from '../components/ServiceCard.astro';
import WorkCard from '../components/WorkCard.astro';
---

<BaseLayout 
  title="フリーランスWeb制作 - 実績と成果を重視した開発サポート"
  description="レガシーシステムの改修からモダンWeb開発まで、確実な成果にこだわるフリーランスエンジニアのポートフォリオサイトです。"
>
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <h1 class="hero-title">
        確実な成果を出す<br />
        Web制作パートナー
      </h1>
      <p class="hero-subtitle">
        レガシーシステムの改修からモダンなWeb開発まで<br />
        実績と数字で証明する、フリーランスエンジニア
      </p>
      <div class="hero-cta">
        <CTAButton text="無料相談はこちら" href="/contact" variant="primary" size="large" />
        <CTAButton text="実績を見る" href="/works" variant="secondary" size="large" />
      </div>
    </div>
  </section>

  <!-- Value Proposition -->
  <section class="section value-props">
    <div class="container">
      <h2 class="section-title">選ばれる3つの理由</h2>
      <div class="props-grid">
        <div class="prop-item">
          <div class="prop-number">01</div>
          <h3>成果重視の開発</h3>
          <p>単なる開発ではなく、ビジネス成果にコミット。パフォーマンス改善、CV率向上など、数字で示せる成果を追求します。</p>
        </div>
        <div class="prop-item">
          <div class="prop-number">02</div>
          <h3>幅広い対応力</h3>
          <p>レガシーシステムの改修から最新技術まで対応可能。既存システムを理解し、段階的な改善提案ができます。</p>
        </div>
        <div class="prop-item">
          <div class="prop-number">03</div>
          <h3>明確なコミュニケーション</h3>
          <p>技術的な内容も分かりやすく説明。進捗の可視化、定期報告で安心してお任せいただけます。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Preview -->
  <section class="section services-preview">
    <div class="container">
      <h2 class="section-title">提供サービス</h2>
      <div class="services-grid">
        <ServiceCard 
          icon="🎨"
          title="Webサイト制作"
          description="コーポレートサイト、LP、ECサイトなど、目的に応じた最適な設計と実装を提供します。"
        />
        <ServiceCard 
          icon="🔧"
          title="既存サイト改修"
          description="レガシーコードの改善、パフォーマンス最適化、機能追加など、既存サイトの価値を高めます。"
        />
        <ServiceCard 
          icon="⚡"
          title="フロントエンド開発"
          description="React, Vue, Astroなど最新技術を活用し、高速で使いやすいUIを実現します。"
        />
      </div>
      <div class="section-cta">
        <CTAButton text="サービス詳細を見る" href="/services" variant="primary" />
      </div>
    </div>
  </section>

  <!-- Works Preview -->
  <section class="section works-preview">
    <div class="container">
      <h2 class="section-title">実績紹介</h2>
      <div class="works-grid">
        <WorkCard 
          title="ECサイトのパフォーマンス改善"
          description="読込時間を3.5秒から1.2秒に短縮。離脱率25%改善、CV率18%向上を実現。"
          tags={['パフォーマンス最適化', 'React', 'Next.js']}
          link="/case-studies/ec-performance"
        />
        <WorkCard 
          title="レガシーシステムのモダナイゼーション"
          description="jQuery中心の古いコードベースをReactに段階移行。保守性向上とバグ70%削減。"
          tags={['リファクタリング', 'React', 'TypeScript']}
          link="/case-studies/legacy-modernization"
        />
        <WorkCard 
          title="コーポレートサイトリニューアル"
          description="Core Web Vitals全項目グリーン達成。問い合わせ数3倍増を実現。"
          tags={['Webサイト制作', 'Astro', 'SEO']}
          link="/case-studies/corporate-renewal"
        />
      </div>
      <div class="section-cta">
        <CTAButton text="すべての実績を見る" href="/works" variant="primary" />
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="container">
      <h2>まずは無料相談から</h2>
      <p>要件が固まっていなくても大丈夫です。課題のヒアリングから丁寧にサポートします。</p>
      <CTAButton text="お問い合わせフォームへ" href="/contact" variant="primary" size="large" />
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero Section */
  .hero {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: var(--spacing-xl) 0;
    text-align: center;
  }

  .hero-title {
    font-size: 3rem;
    margin-bottom: var(--spacing-sm);
    line-height: 1.2;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    margin-bottom: var(--spacing-md);
    opacity: 0.95;
  }

  .hero-cta {
    display: flex;
    gap: var(--spacing-sm);
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Value Props */
  .value-props {
    background-color: var(--bg-light);
  }

  .props-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-md);
  }

  .prop-item {
    background-color: var(--bg-white);
    padding: var(--spacing-md);
    border-radius: 0.5rem;
    box-shadow: var(--shadow);
  }

  .prop-number {
    font-size: 2.5rem;
    font-weight: bold;
    color: var(--primary-color);
    opacity: 0.3;
    margin-bottom: var(--spacing-xs);
  }

  .prop-item h3 {
    margin-bottom: var(--spacing-sm);
  }

  .prop-item p {
    color: var(--text-gray);
    margin-bottom: 0;
  }

  /* Services & Works Grids */
  .services-grid,
  .works-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-md);
  }

  .section-cta {
    text-align: center;
    margin-top: var(--spacing-md);
  }

  /* CTA Section */
  .cta-section {
    background-color: var(--primary-color);
    color: white;
    text-align: center;
    padding: var(--spacing-lg) 0;
  }

  .cta-section h2 {
    margin-bottom: var(--spacing-sm);
    color: white;
  }

  .cta-section p {
    font-size: 1.125rem;
    margin-bottom: var(--spacing-md);
    opacity: 0.95;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }

    .props-grid,
    .services-grid,
    .works-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
